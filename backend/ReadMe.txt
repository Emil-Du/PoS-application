Pakeitimai:

1. pakeistas reservationTime i appointmentTime: GET /api/Reservations ir PATCH /api/Reservations/{reservationId}

2. Prideti RegisterEmployee ir LoginEmployee endpointai.

3. pakeistas login return info (grazina tik emplyee id if role) nes jwt perduodama i cookies

